server {
    listen 80;
    server_name 40.112.93.161;

    location = /favicon.ico { access_log off; log_not_found off; }

    location / {
	root /var/www/react-todo;	
    }	


    location /api {
	include proxy_params;
	proxy_pass http://unix:/home/nikita/todo/todo/todo.sock;
    }
}

